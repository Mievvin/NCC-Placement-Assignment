@page
@model PrescriptionModel
@{
    ViewData["Title"] = "Email a Prescription";
}
@using Microsoft.AspNetCore.Identity
@using NCCWebApp.Models

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
@{
    var usr = await UserManager.GetUserAsync(User);
    var email = usr?.Email;
}

<h1>@ViewData["Title"]</h1>
<h2>On this page you are able to send an email prescription for the condition provided during registration.</h2>
<h3>Your Condition: @UserManager.FindByNameAsync(UserManager.GetUserName(User)).Result.Condition</h3>
<body>
    @{
        var user = ViewData["ApplicationUser"] as NCCWebApp.Models.ApplicationUser;
    }
</body>

<div class="row">
    <div class="col-md-6">
        <form id="profile-form" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="OrderEmail"></label>
                <input asp-for="OrderEmail" />
            </div>
            <button id="update-profile-button" type="submit" class="btn btn-primary">Send Email</button>
        </form>
    </div>
</div>
